
const styles = {
    mapContainer: {
      width: '100%', // Set the width as needed
      height: '400px', // Set the height as needed
      position: 'fixed', // Fixed position
      top: '0', // Optional: To anchor it at the top of the viewport
      left: '0', // Optional: To anchor it at the left of the viewport
      zIndex: '1', // Optional: Adjust z-index as needed
  
      // Limit scrolling
      overflow: 'hidden',
    },
  };
  
  export default styles;
  